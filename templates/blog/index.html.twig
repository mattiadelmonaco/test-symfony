{% extends 'base.html.twig' %}

{% block title %}Home — SymBlog{% endblock %}

{% block body %}
    <h1>SymBlog</h1>

    {% for post in posts %}
        <div>
            <h2>{{ post.title }}</h2>
            <a href="#">Leggi</a>
        </div>
    {% endfor %}
{% endblock %}